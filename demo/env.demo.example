# Demo OpenShift — variables de entorno
#
# Instalación:
#   cp env.demo.example demo.env
#   # Edita OPENSHIFT_AI_TOKEN en demo.env (token del workshop MaaS)
#   ./install.sh

# Namespace donde se despliega el semantic router
DEMO_NAMESPACE=semantic-router-demo

# Gateway MaaS Red Hat Workshops (misma base para los tres modelos)
MAAS_BASE_URL=https://maas-rhdp.apps.maas.redhatworkshops.io

GENERAL_SERVICE_URL=${MAAS_BASE_URL}
CODE_SERVICE_URL=${MAAS_BASE_URL}
PRIVACY_SERVICE_URL=${MAAS_BASE_URL}

# Token MaaS — debe ser el del workshop RHDP (no el del cluster maas.apps.ocp.cloud.rhai-tmm.dev)
# Verifica con: curl -s https://maas-rhdp.apps.maas.redhatworkshops.io/v1/models -H "Authorization: Bearer $TOKEN"
OPENSHIFT_AI_TOKEN=your-token-here

# HuggingFace — requerido para descargar modelos de clasificación del router (mmBERT)
# Crear en https://huggingface.co/settings/tokens (debe empezar por hf_, no yhf_)
HF_TOKEN=your-hf-token-here

# Helm
HELM_RELEASE=semantic-router-demo
HELM_CHART=oci://ghcr.io/vllm-project/charts/semantic-router
HELM_CHART_VERSION=

# StorageClass del cluster (gp3-csi en AWS/OpenShift sandbox)
STORAGE_CLASS=gp3-csi
